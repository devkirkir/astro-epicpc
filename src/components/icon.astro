---
interface Props {
  name: "location-icon" | "cart-icon" | "burger-icon";
}

const { name } = Astro.props;

const { default: innerHTML } = await import(`/public/icons/${name}.svg?raw`);
---

<Fragment set:html={innerHTML} />
